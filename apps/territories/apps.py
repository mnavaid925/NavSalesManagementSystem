from django.apps import AppConfig


class TerritoriesConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.territories"
    verbose_name = "Territory & Quota Management"
